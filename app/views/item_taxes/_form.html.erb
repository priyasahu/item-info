<%= form_with(model: item_tax, local: true) do |form| %>
  <% if item_tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_tax.errors.count, "error") %> prohibited this item_tax from being saved:</h2>

      <ul>
      <% item_tax.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tax_type %>
    <%= form.number_field :tax_type, id: :item_tax_tax_type %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.collection_select :item_id, Item.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :tax %>
    <%= form.text_field :tax, id: :item_tax_tax %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
